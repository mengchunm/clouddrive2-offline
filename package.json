{
  "name": "clouddrive2-extensions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "ci": "pnpm -r run ci",
    "lint": "pnpm -r run lint",
    "format": "pnpm -r run format"
  }
}
