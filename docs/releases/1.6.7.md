# 1.6.7 发布说明

## 任务文件折叠样式

- 可展开任务的名称前显示折叠箭头，箭头随展开状态实时变化。
- 文件详情直接作为任务行的缩进内容展示，去除独立卡片边框与重复任务名称。
- 进入子目录时仅显示相对路径和返回上一级按钮。

## 文件选择与列布局

- 每个文件和目录均可独立勾选，表头复选框支持当前目录全选。
- 文件选择不会影响任务主列表的勾选状态，切换目录或关闭任务时自动清空。
- 文件表采用与主任务表一致的四段布局：`32px` 选择列、弹性名称列、`90px` 信息列、`180px` 操作列。
- 信息列合并显示文件类型与大小，窗口缩放时仅名称列改变可见宽度。
