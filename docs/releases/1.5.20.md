# 1.5.20 发布说明

此版本按拖动方向重新定义任务表头分隔线行为。

## 向右拖动

- 分隔线左侧紧邻列变宽。
- 分隔线右侧的中间列保持原有宽度，只整体向右移动。
- 所需空间统一从最右侧操作列扣除。
- 操作列达到最小宽度后停止继续向右。

## 向左拖动

- 只缩小分隔线左侧紧邻的一列。
- 该列更左侧的所有列宽保持不变。
- 释放的空间由最右侧操作列吸收。
- 左侧紧邻列达到最小宽度后停止继续向左。

拖动预览会同步平移右侧列标题以及其中后续的小分隔线，以直观体现中间列仅改变位置而未改变宽度。
