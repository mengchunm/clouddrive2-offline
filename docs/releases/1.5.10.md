# 1.5.10 发布说明

此版本修复任务表格的实际列布局联动问题。

## 原因

当列配置总宽小于表格可用宽度时，Ant Design 会按比例拉伸选择框、名称、状态和操作列。此时拖动状态列右侧分隔线，不仅该分隔线会移动，状态列左侧边界也会因整体比例变化而移动。

## 修复

- 名称列与状态列严格使用保存的像素宽度，不再被表格按比例缩放。
- 不可拖动的操作列自动吸收窗口内的剩余空间。
- 当名称、状态和操作列的最小总宽超过窗口时，使用任务表格内部横向滚动。
- 展开窗口尺寸变化后自动重新计算剩余空间。
