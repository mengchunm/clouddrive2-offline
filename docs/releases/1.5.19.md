# 1.5.19 发布说明

此版本放宽列宽拖动范围，并优化分隔线样式。

## 拖动范围

- 移除名称列和状态列的人为最大宽度限制。
- 名称/状态分隔线只在名称列或状态列达到各自最小宽度时停止。
- 状态/操作分隔线只在状态列或操作列达到最小宽度时停止。

## 分隔线样式

Ant Table 本身已经绘制灰色列边框，旧版又绘制了一条常驻灰色手柄线，因此看起来像两条线。现在自定义手柄线默认透明，只在悬停或拖动时显示蓝色，并与原生边框对齐。
