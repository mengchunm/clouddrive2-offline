# 1.5.4 发布说明

1.5.4 让缩略悬浮球和展开后的任务面板支持自由拖动。

## 操作方式

- 缩略状态：按住悬浮球拖动；普通点击仍然展开面板。
- 展开状态：按住卡片标题中的 CloudDrive2 区域拖动。
- 设置、收起、标签页、搜索框和任务表格保持正常点击及滚动行为。

## 位置与边界

- 两种状态共用左侧和底部位置锚点，展开面板会从悬浮球位置向上展开。
- 位置保存在扩展全局存储中，切换网站或重新打开浏览器后继续沿用。
- 面板会限制在窗口可视范围内；浏览器窗口缩放后自动校正。
- 小尺寸窗口下，面板宽高不会超过当前视口。
