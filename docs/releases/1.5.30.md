# 1.5.30 发布说明

此版本为任务悬浮窗口增加展开状态恢复。

## 刷新恢复

- 窗口处于展开状态时刷新网页，刷新后仍然展开。
- 窗口处于收起状态时刷新网页，刷新后仍显示悬浮球。
- 展开和收起各自保存的位置继续生效，展开尺寸也继续使用已有配置。
- 浏览器视口较小时仍会临时限制窗口尺寸，视口恢复后按照 1.5.29 的逻辑恢复用户尺寸。

## 状态更新入口

以下行为都会同步保存展开状态：

- 点击悬浮球展开窗口。
- 点击窗口右上角的“收起”。
- 提交离线任务后自动展开窗口。

首次安装没有保存状态时仍默认收起。
