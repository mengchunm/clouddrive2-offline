# 1.5.8 发布说明

1.5.8 为展开悬浮窗口增加尺寸调整和任务表格列宽调整。

## 窗口缩放

- 拖动窗口右边缘调整宽度。
- 拖动窗口上边缘调整高度。
- 拖动右上角同时调整宽度和高度。
- 窗口尺寸会保存，并自动限制在当前浏览器可视范围内。
- 增加窗口高度后，任务列表会自动显示更多行。

## 表格列宽

- 将鼠标移动到“名称 / 状态 / 操作”表头右侧分隔线并拖动。
- 向右拖动“名称”分隔线可以显示更完整的任务名称。
- 三列宽度分别保存，重新打开网页后继续使用。
- 当列总宽度超过窗口时，只在任务表格内部出现横向滚动，不会产生面板外层滚动条。
