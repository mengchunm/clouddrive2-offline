# 1.6.36 发布说明

发布日期：2026-07-25

- 首次内嵌字幕读取范围从约 130 秒缩短为当前位置附近 17 秒，优先显示当前字幕，不再先读取一分钟左右的 4K 音视频数据。
- 第一段显示后，播放器会以较短的重叠窗口在后台继续预读；重叠区间不会重复触发同一范围。
- libav WebAssembly 在字幕 Worker 内只初始化一次，后续分段提取和跳转复用同一实例。
- 正常的音频解码取消，以及空格播放后紧接暂停产生的 `AbortError`，不再显示为控制台错误。

本次只更新 `packages/extension/build`，不生成 ZIP。重新加载扩展后请关闭旧播放页并重新打开，确保旧字幕 Worker 已退出。
