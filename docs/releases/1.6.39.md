# 1.6.39 发布说明

发布日期：2026-07-25

- 音频和默认字幕仍然并行加载，但音轨先获得最多 3 秒的容器元数据探测优先期；探测完成后首段 PCM 与字幕同时继续。
- Range Host 最多并发四路请求，并按音频、字幕、普通读取的顺序调度。
- 音频与字幕访问相同视频分块时继续共享内存缓存和进行中的请求，不会重复下载。
- 手动点击字幕无需等待音轨探测，立即加载。

该方案只保护音频启动的关键路径，不会等到整段音频完成后才开始字幕。

本次只更新 `packages/extension/build`，不生成 ZIP。重新加载扩展后请关闭旧播放页并重新打开。
