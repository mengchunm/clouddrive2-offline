# 1.5.24 发布说明

此版本取消内部列分隔线对外部悬浮窗口尺寸的影响。

## 内部分隔线

- 向右拖动时，先压缩紧邻右侧列，再压缩最右侧操作列。
- 两个右侧列均达到最小宽度后，蓝色长线停止继续向右。
- 拖动不会再改变悬浮窗口宽度或左侧位置。

## 外部窗口

- 只有窗口右边缘或右上角缩放控件可以改变窗口宽度。
- 外部窗口缩小时，仍会联动压缩名称列和状态列。
- 名称、状态和操作列全部达到最小宽度后，窗口才停止继续缩小。

即联动方向固定为“外部窗口影响内部列”，内部列不再反向影响外部窗口。
