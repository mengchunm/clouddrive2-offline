# 1.5.18 发布说明

此版本修复列标题拖动预览没有稳定实时显示的问题。

## 实时反馈

- 拖动小分隔线时，直接对该分隔线应用 GPU 横向位移。
- 分隔线右侧的“状态”或“操作”标题使用相同位移同步跟随。
- 位移每个屏幕刷新帧更新一次，不依赖 Ant Table 的列宽重新测量。
- 松手时将预览位置固化为真实表头和任务行列宽，并立即清除临时位移。

因此，拖动过程中看到的标题位置就是最终落点，而不是等拖动结束后才出现变化。
