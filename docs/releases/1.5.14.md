# 1.5.14 发布说明

此版本统一窗口缩放和表头分隔线的最小宽度约束。

## 窗口缩放

窗口最小宽度由选择框、当前名称列、当前状态列、操作列最小宽度以及面板边距共同决定。向左缩小窗口时，右边缘不会越过操作列最右侧。

## 分隔线

- 名称/状态分隔线仍在两列之间交换宽度，不改变操作列。
- 状态/操作分隔线根据当前窗口可用宽度限制移动范围。
- 操作列达到最小宽度后，分隔线停止移动，不会继续把操作按钮挤出窗口。
