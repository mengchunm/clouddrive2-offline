# 1.5.3 发布说明

1.5.3 调整任务搜索的触发方式和进度反馈。

## 搜索操作

- 输入关键词后点击“搜索”按钮，或按 Enter 执行。
- 继续编辑输入框不会自动请求或改变当前结果。
- 点击输入框清除按钮会立即恢复普通任务列表。

## 进度反馈

建立全量索引时显示：

- 当前已读取页数和总页数。
- 已读取任务数和任务总数。
- 可视化百分比进度条。

索引完成后，状态区域显示匹配任务数量和当前搜索关键词。缓存命中时会直接进入结果状态，不重复播放虚假进度。
