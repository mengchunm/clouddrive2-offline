# 1.5.16 发布说明

此版本进一步优化快速缩放和列分隔线拖动体验。

## 分隔线拖动

拖动期间不再实时重排包含全部任务行和操作按钮的表格，而是显示一条由 GPU 合成层移动的蓝色引导线。引导线持续跟随鼠标，并遵守列最小宽度限制；松开鼠标时才一次性提交最终列宽。

## 窗口缩放

Ant Table 的固定表头和表体由两张独立表格组成，快速缩放时两者的尺寸测量可能不同步。缩放开始时现在会记录并冻结两张表的横向宽度，缩放结束后再统一适配最终窗口，从而减少列标题和分隔线晃动。

列表的垂直弹性布局不受影响，改变窗口高度时仍会实时显示或隐藏任务行。
