# 1.5.9 发布说明

此版本优化任务列表列宽拖动体验。

## 修复

- 分隔线始终调整其左侧列：名称后的分隔线调整名称列，操作前的分隔线调整状态列。
- 修复拖动操作前分隔线时，状态列前方边界被挤动的现象。
- 操作列是最后一列，不再在其右侧显示无实际必要的拖拽分隔线。

## 性能

拖动过程中仅更新表格 CSS 宽度变量，不再在每次鼠标移动时触发整张 React 表格重新渲染；松开鼠标后才保存最终列宽。
