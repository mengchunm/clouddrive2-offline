# 1.5.31 发布说明

此版本为任务列表增加浏览进度恢复。

## 保存内容

- 当前分页页码。
- 当前页任务表体的纵向滚动位置。

刷新网页或收起后重新展开悬浮窗口时，会先加载保存的任务页，待任务行渲染完成后再恢复滚动位置。

## 边界处理

- 切换页码时，新页面从顶部开始并立即保存新页码。
- 如果删除任务后原页码超过最新总页数，自动切换到当前最后一页。
- 滚动事件按 `150ms` 节流写入扩展存储，页面离开和组件卸载时补写最终值。
- 快速翻页时，只允许当前目标页的恢复帧修改滚动位置。

搜索操作仍沿用原规则：执行或清空搜索时回到第一页。
