# 1.6.5

此版本继续加固本地文件定位助手的检测与测试。

- 浏览器现场验证任务文件操作区已经渲染本地定位按钮。
- Native Host 测试通过与 Chrome 相同的 CMD 启动层，并验证协议 5 的 `ping` 和路径拒绝逻辑。
- 检测成功会同步保存启用状态，后续任务列表可立即复用。
- 瞬时超时或连接关闭不再清空已安装状态；明确未注册、禁止访问或协议过旧时才关闭按钮。
- 控制台会输出连接成功、失败或异常的明确原因。
