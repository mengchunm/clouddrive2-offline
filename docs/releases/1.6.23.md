# 1.6.23 发布说明

- 子文件夹名称在鼠标经过时不再显示下划线，悬停前后保持相同文字样式。
- 视频文件类型图标由摄像机轮廓替换为方框播放图标，和目录树内其他文件类型图标保持一致。
- 实际播放操作按钮及播放逻辑没有变化。
- 刷新当前网页时恢复已打开的悬浮播放器、视频比例、精确进度和播放状态。
- 增加扩展自有的视频 Range 分片缓存：刷新后复用已加载的视频内容，默认上限 2 GiB，可在设置中关闭或清空；不支持 Range 或缓存异常时自动退回直连播放。
