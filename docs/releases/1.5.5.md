# 1.5.5 发布说明

1.5.5 修复缩略悬浮球不能可靠拖动的问题。

## 原因

悬浮球内部图片可能启动浏览器原生图片拖拽，从而抢占按钮上的 Pointer Move。鼠标快速移出36像素按钮范围时，依赖元素自身监听也不够稳定。

## 修复

- 图片禁止原生拖动并不再接收指针事件。
- 按下后由窗口持续监听移动、抬起和取消事件。
- 使用 Pointer Capture 保持鼠标、触摸和触控笔行为一致。
- 未超过拖动阈值时仍视为普通点击并展开面板。
