# 1.6.3

此版本修复本地文件定位助手的安装、卸载状态同步。

- 内容脚本持续同步扩展存储缓存；安装完成后，无论任务列表当时是否已挂载，后续打开悬浮窗口都能读取最新启用状态。
- 已打开的任务列表继续通过存储变更事件实时显示或隐藏本地定位图标。
- 卸载成功后，后台立即断开持久 Native Messaging 连接。
- 新版 PowerShell Host 返回卸载结果后立即结束进程，避免旧进程被再次误判为已安装。
