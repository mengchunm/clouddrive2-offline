# 1.5.38

发布日期：2026-07-22

## 安装与卸载容错

- 下载安装脚本时不再强制覆盖同名文件；Chrome 会自动添加序号，避免目标文件被占用或残留时取消下载。
- 浏览器取消 CMD 下载时显示明确中文提示，引导用户重新下载并允许保留脚本。
- 卸载与状态检测统一复用后台 Native Messaging 持久连接，避免旧 Host 进程仍连接、注册项却已经不存在时产生矛盾状态。
- 如果注册项或 Host 本来就不存在，卸载操作按完成处理，并同步隐藏任务列表中的本地目录按钮。
