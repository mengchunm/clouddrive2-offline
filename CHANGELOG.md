# 变更记录

本项目遵循语义化版本编号。当前主要发布版本以 `packages/extension` 的版本为准。

## 1.6.42 - 2026-07-25

- 修复播放器初始化或恢复进度触发 `seeking` 时，误取消尚未建立活动状态的首次 libav 字幕提取，导致出现 Matroska `Read error` 后整条字幕轨不再显示的问题。
- 字幕取消范围收窄为已经成功加载后的后台分段预读；首次字幕加载仅在用户切换轨道、关闭播放器或超时时取消。
- 后台预读增加请求代次校验，旧取消任务的 `finally` 不会覆盖跳转后新请求的加载状态。
- 保留 1.6.41 的兼容音频宿主恢复方案与此前的 Range 优先级优化。
- 本版本直接更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.41 - 2026-07-25

- 修复 1.6.40 中兼容音频宿主可能无法完成握手、最终同时报告后台预热和音频初始化超时的回归。
- 回滚“媒体缓存注册完全后台化”的启动时序，恢复 1.6.39 已验证可正常出声的音频初始化屏障；字幕取消、Range 优先级提升和字幕范围合并继续保留。
- 音频 iframe 首次握手失败时会清理失效实例并自动重建一次，同时区分宿主页加载超时与宿主脚本未响应。
- 音频宿主先完成轻量 `host-ready` 握手，再注册 AC-3/E-AC-3 解码器；同步注册错误会返回明确错误，不再伪装成宿主启动超时。
- 本版本直接更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.40 - 2026-07-25

- 内嵌字幕解封装新增端到端取消协议；拖动进度、切换字幕或关闭播放器时，过期任务会从播放器经扩展桥传递到 libav Worker，不再阻塞当前位置的新请求。
- Range Host 对尚未开始的同一分片支持优先级提升；字幕先排队、音频随后命中同一分片时，会把该任务提升为音频优先，同时继续保持最多四路并发。
- 字幕超时和正常取消不再显示为加载失败通知，也不会留下失效的事件监听。
- 播放器与兼容音频不再等待仅维护元数据的 IndexedDB 媒体缓存注册，已知文件大小直接进入画面和音轨初始化；注册仍在后台完成。
- 连续字幕预读窗口会合并覆盖区间，避免长时间播放时 `ranges` 列表无限增长。
- 移除已经内嵌到 `libav-worker.js` 的独立 libav WASM 重复副本，扩展构建减少约 1.85 MiB，并收紧公开资源范围。
- 新增字幕取消链路和 Range 优先级提升的回归约束。
- 本版本直接更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.39 - 2026-07-25

- 默认字幕与兼容音频改为分阶段并行：音轨只先完成容器元数据探测（最多独占 3 秒），随后首段 PCM 解码与默认字幕当前窗口并行，不等待完整音频段或首个 PCM 才启动字幕。
- Range Host 新增最多四路并发的优先级调度；音频请求优先于字幕和普通读取，字幕仍持续加载，不采用全局串行或暂停方案。
- 音频、字幕请求继续共享同一 1 MiB 分块缓存；两者访问相同 MKV 区域时复用已完成或进行中的网络请求，避免重复下载。
- 用户手动选择字幕不经过自动加载屏障，仍立即响应。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.38 - 2026-07-25

- 字幕改为默认开启：首次播放自动选择评分最高的完整字幕，优先外挂字幕、容器默认标记和中文/英文完整轨，同时降低 forced 与 CC/SDH 轨的自动选择优先级。
- 用户对每个视频选择的内嵌、外挂或关闭字幕状态使用稳定文件路径持久保存；刷新页面、直链 token 更新及以后再次打开同一视频时自动恢复。
- MKV 快速元数据解析新增 TrackFlagDefault 与 TrackFlagForced；目标视频已确认 `English (forced)` 同时标记为 default/forced，而普通 `English` 是完整非 forced 轨，因此自动选择普通 English。
- forced 字幕仍可手动选择并记忆；该轨只在外语、标牌或必要对白处有内容，普通时间段为空属于文件轨道本身的语义，不是加载失败。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.37 - 2026-07-25

- 针对目标 24.4 GB MKV 实测 CloudDrive2 单路 Range 约 1 MiB/s：首次字幕读取进一步缩短到当前位置前后各 1 秒，避免原 17 秒窗口仍需下载几十 MiB 并占满近一分钟。
- 后续字幕改为前 3 秒、后 8 秒的短重叠窗口，与首段音频解码范围接近，使音频和字幕更容易复用同一批 1 MiB Range 缓存，减少竞争。
- 音频宿主探测超时调整为 90 秒，播放器外层等待为 100 秒，避免内外两层在相同的 60 秒边界同时失效；正常情况下短字幕窗口会让音轨探测远早于该上限完成。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.36 - 2026-07-25

- 内嵌字幕首次提取窗口由当前位置附近约 130 秒缩短为 17 秒，避免解封装 4K 高码率 MKV 的大量视频和音频数据后才显示第一条字幕。
- 首次字幕显示后以 45 秒重叠窗口后台预读；覆盖区间选择改为使用结束时间最远的区间，避免重叠窗口重复提取。
- libav WebAssembly 实例在字幕 Worker 生命周期内单例复用，后续预读和跳转不再重复编译、实例化 WASM；提取任务串行执行，避免共享虚拟文件系统互相覆盖。
- 音频预读的正常取消与播放/暂停竞争产生的 `AbortError` 不再作为控制台错误输出。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.35 - 2026-07-24

- libav 字幕 Worker 现在直接内嵌 libav 工厂与 WebAssembly 字节，不再从 `chrome-extension://.../libav/*.wasm` 发起运行时请求，消除实际 Chrome 中 `Failed to fetch`、XHR 加载失败及后续字幕预读中断。
- 音频沙箱宿主改为经典 IIFE 入口，避免 opaque-origin 沙箱对 ES Module 入口加载和初始化握手的影响；`@mediabunny/ac3` 内部 Blob Worker 与 WASM 仍完全位于扩展包内。
- 新增回归约束，防止字幕重新使用 `wasmurl`/`toImport` 动态加载，或音频沙箱重新改回模块入口。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.34 - 2026-07-24

- 音频沙箱宿主页先注册消息端口并返回 `host-ready`，随后再延迟启动 E-AC-3 解码器预热，避免首次有效预热的同步初始化阻塞 iframe 握手并触发启动超时。
- 如果正式媒体已经开始打开，则跳过独立预热任务，由正式解码路径完成相同初始化，避免冷启动时两个解码任务争用资源。
- 音频宿主页握手超时提高到 30 秒；失败时会关闭 MessagePort 并移除失效 iframe，后续请求可以干净重试。
- 本地文件助手状态检测超时由 3 秒调整为 10 秒，并合并十秒内的重复焦点检测；扩展重载造成的消息通道关闭不再输出无意义异常。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.33 - 2026-07-24

- 修复 libav 在文件尾部请求放大到 1 MiB 时，Range Host 把 CloudDrive2 合法的 EOF 短响应误报为“Range 数据提前结束”，导致字幕读取回退或中断的问题。
- Range Host 现在按服务端返回的文件总长度截断请求，并向调用方返回准确的实际 Content-Range。
- 修复内置 E-AC-3 预热样本多余 Base64 填充导致的 `InvalidCharacterError`，并增加严格 Base64 与 Matroska 文件头回归检查。
- 选择当前时间窗没有事件的 forced 字幕时，播放器会短暂提示该轨当前无内容，不再让合法空轨看起来像加载故障。
- 已验证目标视频在约 28:10 的 forced 字幕轨没有字幕事件，而普通 English、English (CC) 和西班牙语轨均包含连续字幕；扩展保留用户选择的轨道语义，不把 forced 自动替换为完整字幕。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.32 - 2026-07-24

- MKV 内嵌 SubRip（`S_TEXT/UTF8`）和 WebVTT 字幕改由扩展内 libav.js 按当前播放位置分段解封装，不再回退到扫描整部远程 MKV 的旧提取器。
- SubRip 数据包使用 PTS 与 duration 在 Worker 内组装为标准 WebVTT，播放接近分段边界或跳转进度时继续按需读取、去重、排序和更新字幕轨。
- 字幕轨当前两分钟没有事件时返回有效空字幕并继续预读，兼容电影开头无对白以及内容稀疏的 forced 字幕，不再误报字幕为空。
- Matroska 回归测试同时覆盖 ASS 和 SubRip 双字幕轨。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.31 - 2026-07-24

- 修复音频共享读取误命中 40 字节静态占位文件，导致 Mediabunny 报告 `unexpected length` 并无法初始化兼容音轨。
- 新增非沙箱扩展 Range Host，凭扩展权限直接读取 CloudDrive2，以可转移 ArrayBuffer 向音频和 libav 字幕提供数据；失败时自动回退后台代理。
- Range Host 使用 1 MiB 对齐分块、64 MiB 内存 LRU 和并发请求合并，兼顾首段响应速度与音频/字幕复用。
- 原生视频暂时保持使用 CloudDrive2 URL，不再指向 MV3 无法动态代理的扩展静态资源。
- E-AC-3 预热失败会延迟 250ms 重试一次，并输出具体 DOMException 名称；预热失败不阻止正式播放按需初始化。
- Native Host 请求超时后主动断开陈旧连接，后续检测会重新启动本地助手，而不是持续复用卡住的端口。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.30 - 2026-07-24

- 视频、AC-3/E-AC-3 音频和 libav 字幕统一读取扩展的 4 MiB 媒体分块缓存；同一分块的并发请求只访问一次 CloudDrive2。
- 缓存地址的音频和字幕 Range 数据直接以可转移 `ArrayBuffer` 传递，移除热路径上的 Base64 编解码；缓存不可用时仍自动回退原有代理。
- 新增 64 MiB 内存热分块 LRU，网络数据完成后立即交给播放器，IndexedDB 改在后台顺序持久化，不再阻塞首帧和首段音频。
- 任务文件已有大小直接用于注册媒体缓存，省去重复的 `bytes=0-0` 探测；临时直链更新但文件路径与大小未变时继续复用已有分块。
- 继续使用扩展内专用 FFmpeg WebAssembly AC-3/E-AC-3 解码器；通用 ffmpeg.wasm CLI 不进入播放热路径，避免虚拟文件系统和整包启动开销。
- 本版本仅更新 `packages/extension/build`，不生成 ZIP 归档。

## 1.6.29 - 2026-07-24

- 取得 CloudDrive2 视频直链后立即开始 MKV 音轨探测和首段解码，与播放列表、字幕解析并行，不再等到 ArtPlayer 创建后才启动。
- 直接复用任务文件已有的准确文件大小，取消音频兼容层额外的 `bytes=0-0` 文件长度请求。
- 本版本仅更新开发构建目录，不生成新的 ZIP 归档。

## 1.6.28 - 2026-07-24

- 展开任务悬浮窗时，在隐藏沙箱中使用扩展包内置的极小静音 E-AC-3 样本预先创建 Worker、编译 WebAssembly 并初始化解码器。
- 用户选择 MKV 后，音轨探测和首段 PCM 解码会在 ArtPlayer 与视频缓存地址初始化期间并行执行，播放器创建后直接接管已产出的音频块。
- 画面加载始终独立，不会为了音频初始化、解码或同步而暂停或延后。

完整说明见 [`docs/releases/1.6.28.md`](docs/releases/1.6.28.md)。

## 1.6.27 - 2026-07-24

- 首个 E-AC-3/AC-3 解码帧立即交给 Web Audio，不再等待首个约 0.5 秒聚合块。
- 播放器处于暂停状态时后台预解码当前位置；恢复播放可直接使用已准备的 PCM，但不会为了等待音频而阻塞或暂停画面。
- 用户播放早于预热完成时继续保持画面优先，声音在首帧解码完成后以当前视频时间立即追上。

完整说明见 [`docs/releases/1.6.27.md`](docs/releases/1.6.27.md)。

## 1.6.26 - 2026-07-24

- 将 AC-3/E-AC-3 解码输出由“等待完整十秒分段”改为约 0.5 秒 PCM 流式分块，首块完成后立即播放。
- 跳转进度后同样按小块恢复声音，后续音频继续在已有十秒 Range 解码任务中排队，避免用大量短任务反复初始化解码器。

完整说明见 [`docs/releases/1.6.26.md`](docs/releases/1.6.26.md)。

## 1.6.25 - 2026-07-24

- 修复远程 AC-3/E-AC-3 首段解码期间被同步定时器持续作废，导致兼容层已启用但始终无声的问题。
- 首段或断流重取尚在进行时不再执行时钟漂移校正，解码完成后才开始正常排程和同步。

完整说明见 [`docs/releases/1.6.25.md`](docs/releases/1.6.25.md)。

## 1.6.24 - 2026-07-24

- 为 Chrome 无法原生播放的 MKV AC-3/E-AC-3 音轨增加纯扩展 WebAssembly 解码兼容层。
- 音频使用 CloudDrive2 HTTP Range 分段读取、浏览器内多声道下混，并跟随视频播放状态和时间轴。

完整说明见 [`docs/releases/1.6.24.md`](docs/releases/1.6.24.md)。

## 1.6.23 - 2026-07-22

- 移除子文件夹名称在鼠标悬停时出现的下划线，目录树文字样式保持稳定。
- 视频文件类型图标由摄像机改为更简洁的方框播放图标。

完整说明见 [`docs/releases/1.6.23.md`](docs/releases/1.6.23.md)。

## 1.6.22 - 2026-07-22

- 移除任务目录树状态列中的“视频”“文件夹”等类型标签，目录行不再显示占位短横线。
- 文件类型改由名称前的图标表达，覆盖目录、视频、音频、图片、字幕、压缩包、PDF、文档、代码、链接/播放列表和未知文件。
- 状态列仅为普通文件保留文件大小，折叠箭头、类型图标与名称继续保持统一缩进。

完整说明见 [`docs/releases/1.6.22.md`](docs/releases/1.6.22.md)。

## 1.6.21 - 2026-07-22

- 移除任务目录树的全部引导竖线及相关悬停、选中显示逻辑。
- 第一级任务文件统一增加一级缩进，后续嵌套层级按相同固定间距递增，使目录与文件排列一致。

完整说明见 [`docs/releases/1.6.21.md`](docs/releases/1.6.21.md)。

## 1.6.20 - 2026-07-22

- 任务内文件改为 IDE 风格的懒加载目录树，支持在列表中同时展开多层嵌套目录。
- 折叠按钮、目录缩进和引导竖线均位于复选框之后，状态与操作列继续保持对齐。
- 鼠标位于树节点范围时显示完整祖先引导线；移开后仅为最深层已选文件保留直接父目录竖线，未选择文件时隐藏全部竖线。
- 目录收起后保留已加载节点和勾选状态，重新展开无需再次读取。

完整说明见 [`docs/releases/1.6.20.md`](docs/releases/1.6.20.md)。

## 1.6.19 - 2026-07-22

- 刷新任务列表时直接在刷新按钮内显示旋转状态和“刷新中”文字，不使用额外通知。
- 刷新期间阻止重复点击，并与表格首次加载状态分离，使用缓存时也能获得即时反馈。

完整说明见 [`docs/releases/1.6.19.md`](docs/releases/1.6.19.md)。

## 1.6.18 - 2026-07-22

- 折叠按钮改为无底色的中性箭头，移除蓝色文字和蓝色悬停效果，仅保留轻微明暗反馈。

完整说明见 [`docs/releases/1.6.18.md`](docs/releases/1.6.18.md)。

## 1.6.17 - 2026-07-22

- 移除任务名称的鼠标悬浮完整文本提示，过长名称继续使用列表内省略显示。

完整说明见 [`docs/releases/1.6.17.md`](docs/releases/1.6.17.md)。

## 1.6.16 - 2026-07-22

- 任务文件仅可通过任务名称或名称前方的折叠按钮展开、收起，状态、操作及行内空白区域不再误触。
- 放大折叠按钮，并增加淡蓝底色、悬停和键盘焦点反馈，使可展开任务更容易辨认。

完整说明见 [`docs/releases/1.6.16.md`](docs/releases/1.6.16.md)。

## 1.6.15 - 2026-07-22

- 任务列表允许同时展开多个已完成任务，不再自动收起上一个任务。
- 每个展开任务独立保存当前目录、文件列表、加载状态和内部勾选，互不覆盖。
- 收起任务后保留内部勾选及父任务半选/全选状态，再次展开时原样恢复。
- 统一批量删除支持同时处理多个任务中的半选文件。

完整说明见 [`docs/releases/1.6.15.md`](docs/releases/1.6.15.md)。

## 1.6.14 - 2026-07-22

- 统一外层任务和内部文件的复选状态：内部部分选中时父任务半选，全部选中时父任务选中。
- 点击外层任务复选框会全选或清空当前展开目录中的文件，并在异步加载完成后保持同步。
- 移除折叠区域内的“删除所选”，统一使用任务列表顶部批量删除入口；半选任务仅删除选中的内部文件。

完整说明见 [`docs/releases/1.6.14.md`](docs/releases/1.6.14.md)。

## 1.6.13 - 2026-07-22

- 合并重复的“网页播放”和“ArtPlayer”选项，仅保留“网页播放”，内部继续调用 ArtPlayer。
- 从播放器菜单移除 VLC 和 IINA，保留网页播放、PotPlayer、弹弹Play和 Infuse。
- 自动将旧版本中保存的已移除播放器选项回退为网页播放。

完整说明见 [`docs/releases/1.6.13.md`](docs/releases/1.6.13.md)。

## 1.6.12 - 2026-07-22

- 修复 Ant Design 的白色表格背景覆盖任务文件折叠区域淡蓝色背景的问题。
- 内层表格、容器和单元格现在强制保持透明，折叠层级底色可以稳定显示。

完整说明见 [`docs/releases/1.6.12.md`](docs/releases/1.6.12.md)。

## 1.6.11 - 2026-07-22

- 加深任务文件折叠区域的淡蓝色背景，使折叠内容与外层任务更容易区分。
- 内部文件操作统一为 CloudDrive2 定位、本地定位、播放、下载、删除的顺序。
- 内外播放器按钮共用图标规则，网页播放统一使用圆形播放图标。

完整说明见 [`docs/releases/1.6.11.md`](docs/releases/1.6.11.md)。

## 1.6.10 - 2026-07-22

- 移除任务文件折叠表自身的纵向滚动区域，避免其额外预留滚动条宽度。
- 内部文件的选择、名称、状态和操作列现在与外层任务表严格对齐，并统一跟随外层列表滚动。

完整说明见 [`docs/releases/1.6.10.md`](docs/releases/1.6.10.md)。

## 1.6.9 - 2026-07-22

- 移除任务折叠内容自身的列标题，内部文件直接沿用外层“名称 / 状态 / 操作”表头。
- 修复展开行仍继承嵌套表格内边距的问题，展开内容与主表左右边界严格对齐。
- 内部表格强制复用主表的选择列、弹性名称列、90px 状态列和 180px 操作列。
- 子目录工具栏不再显示路径标题，仅保留“返回上一级”和已选择操作。

完整说明见 [`docs/releases/1.6.9.md`](docs/releases/1.6.9.md)。

## 1.6.8 - 2026-07-22

- 去除任务文件折叠内容的左侧缩进，改用淡色背景区分层级，并继续沿用主表列布局。
- 勾选或取消外层任务时，同步全选或清空当前已加载的内部文件；先勾选后展开同样生效。
- 内部文件和目录新增单项删除按钮，并支持勾选多个项目后批量删除。
- 文件删除改用 CloudDrive2 的文件删除接口，不与“删除离线任务”逻辑混用。

完整说明见 [`docs/releases/1.6.8.md`](docs/releases/1.6.8.md)。

## 1.6.7 - 2026-07-22

- 任务名称增加展开/收起箭头，文件详情改为与任务主表一致的折叠表格样式。
- 展开任务根目录时不再重复显示任务名称，进入子目录后仅显示相对路径与返回入口。
- 任务文件新增独立复选框和全选功能，并显示当前已选择数量。
- 文件详情合并为名称、信息、操作三列，列宽规则与主任务表保持一致。

完整说明见 [`docs/releases/1.6.7.md`](docs/releases/1.6.7.md)。

## 1.6.6 - 2026-07-22

- 任务主列表的已完成任务新增本地文件管理器定位图标，无需先展开文件列表。
- 修复 CloudDrive2 Windows 盘符挂载的 `localMount=false` 被错误过滤，导致提示“不在任何已挂载目录中”的问题。
- 新增基于真实 `Z: → /115open` 返回结构的本地路径映射自动测试。

完整说明见 [`docs/releases/1.6.6.md`](docs/releases/1.6.6.md)。

## 1.6.5 - 2026-07-22

- Native Host 自动测试改为经过真实 CMD 启动器和二进制消息管道，不再只测 PowerShell 源文件。
- 助手检测成功后持久保存启用状态；普通瞬时连接失败不再错误隐藏本地定位图标。
- 仅在明确未注册、被禁止或协议过旧时关闭按钮，并增加可诊断的连接日志。

完整说明见 [`docs/releases/1.6.5.md`](docs/releases/1.6.5.md)。

## 1.6.4 - 2026-07-22

- 任务列表加载和窗口重新获得焦点时直接检测 Native Host，不再依赖设置页写入的缓存状态。
- 修复助手已注册但未先打开设置页时，行内文件列表仍不显示本地定位图标的问题。
- 安装脚本改为在同一个 PowerShell 进程中完成解压和注册，并将 Host 文件安装在下载脚本所在目录。

完整说明见 [`docs/releases/1.6.4.md`](docs/releases/1.6.4.md)。

## 1.6.3 - 2026-07-22

- 修复安装本地助手后已打开网页没有及时显示本地定位图标的问题。
- 修复卸载完成后旧 Host 进程继续响应检测，导致设置页刷新又显示“已安装”的问题。
- 卸载响应返回后主动断开后台持久连接，新版 Host 自身也会立即退出。

完整说明见 [`docs/releases/1.6.3.md`](docs/releases/1.6.3.md)。

## 1.6.2 - 2026-07-22

- 恢复随扩展打包的本地文件定位脚本、Native Messaging 通信和设置页下载/卸载入口。
- 本地定位入口仅显示在任务行内展开的文件列表中；任务操作列不恢复旧的“打开本地挂载目录”按钮。
- 旧版助手提示改为简洁的更新提示，不再显示“助手版本过旧，功能未启用”。

完整说明见 [`docs/releases/1.6.2.md`](docs/releases/1.6.2.md)。

## 1.6.1 - 2026-07-22

- 点击已完成任务即可在任务列表行内展开文件，不再占用操作列按钮，也不打开独立窗口。
- 文件夹浏览、格式、大小、播放、CloudDrive2 定位和单文件下载继续保留在行内区域。
- 移除本地挂载目录助手、设置提示、Native Messaging 权限及安装资源。

完整说明见 [`docs/releases/1.6.1.md`](docs/releases/1.6.1.md)。

## 1.6.0 - 2026-07-22

- 完成任务新增“查看任务文件”入口，支持按层浏览文件夹和子目录，避免大型任务一次性递归加载。
- 文件详情显示文件名、格式分类和精确大小，覆盖视频、音频、图片、字幕、压缩包、文档及未知格式。
- 视频和音频可使用当前播放器播放；所有普通文件均可单独下载或在 CloudDrive2 中定位。
- 本地助手新增文件定位动作，可在 Windows 文件资源管理器中选中具体文件；目录仍直接打开。
- Native Host 内部协议升级，旧助手会提示重新下载安装脚本。

完整说明见 [`docs/releases/1.6.0.md`](docs/releases/1.6.0.md)。

## 1.5.39 - 2026-07-22

- 区分按钮的“处理中”和“不可用”状态，修复卸载完成后禁用按钮仍显示等待光标的问题。
- 卸载成功或助手本来就不存在时显示明确的完成提示。

完整说明见 [`docs/releases/1.5.39.md`](docs/releases/1.5.39.md)。

## 1.5.38 - 2026-07-22

- 安装脚本下载改为自动生成不冲突的文件名，避免覆盖正在使用或残留的同名脚本时被 Chrome 取消。
- 将 `USER_CANCELED` 转换为可操作的中文提示，不再直接显示浏览器内部错误码。
- 卸载操作改为复用检测成功的后台 Native Messaging 持久连接，修复状态显示已安装但卸载时报告 Host 不存在的问题。
- Host 已不存在时按卸载成功处理，自动关闭本地目录功能。

完整说明见 [`docs/releases/1.5.38.md`](docs/releases/1.5.38.md)。

## 1.5.37 - 2026-07-22

- 移除“打开本地挂载目录”的独立启用开关，避免安装状态与用户开关互相覆盖。
- 设置页仅保留“下载安装脚本”和“卸载本地助手”两个操作；检测到助手即自动启用，卸载后即自动关闭。
- Native Host 检测统一通过后台持久连接执行，避免设置页检测与实际打开目录使用不同连接而产生状态不一致。
- 安装脚本下载后持续检测，并在设置页重新获得焦点时立即复检。

完整说明见 [`docs/releases/1.5.37.md`](docs/releases/1.5.37.md)。

## 1.5.36 - 2026-07-21

- 重做本地目录助手设置界面，新增明确的“下载安装脚本”“运行安装脚本”和“重新检测”按钮。
- 开关不再隐式触发耗时下载；助手未安装时立即提示先完成安装。
- Native Host 检测增加短超时和独立状态标签，避免设置页长时间无反馈。
- 安装器固定命名为 `clouddrive2-native-host-installer.cmd`，不再向用户暴露协议版本后缀。
- 下载与运行拆分为两个可见步骤，安装完成后自动轮询并启用功能。

完整说明见 [`docs/releases/1.5.36.md`](docs/releases/1.5.36.md)。

## 1.5.35 - 2026-07-21

- 修复 Chrome 启动批处理 Native Host 时空参数被误判为用户安装流程的问题。
- 安装器新增独立的无交互 Native Messaging 启动 CMD，注册表不再直接指向安装器。
- 专用启动器不包含安装分支、提示或暂停，始终直接进入持久消息循环。
- 助手协议升级为 v4，并使用新下载文件名避开已经安装的 v3。

完整说明见 [`docs/releases/1.5.35.md`](docs/releases/1.5.35.md)。

## 1.5.34 - 2026-07-21

- 修复新版 Native Host 安装器仍把注册表指回同目录旧版 CMD 的路径硬编码问题。
- Host 清单现在写入用户实际运行的安装脚本绝对路径。
- 助手协议升级为 v3，并使用新文件名强制避开已经下载的有缺陷 v2 安装器。
- 设置页只把协议 v3 识别为最新版，确保旧 CMD 不会继续冒充已升级状态。

完整说明见 [`docs/releases/1.5.34.md`](docs/releases/1.5.34.md)。

## 1.5.33 - 2026-07-21

- 扩展重载导致旧内容脚本上下文失效时，存储写入不再产生未捕获异常。
- 设置页明确识别 Native Host v1，并提示运行新下载的 v2 安装脚本。
- 保留内存中的最新设置值，同时在失效上下文中停止继续访问扩展存储。

完整说明见 [`docs/releases/1.5.33.md`](docs/releases/1.5.33.md)。

## 1.5.32 - 2026-07-21

- 后台预取 CloudDrive2 挂载点并缓存 5 分钟，避免每次点击重复查询。
- 优先由离线目标路径和任务名称直接推算本地路径，仅在失败时回退到精确文件查询。
- Native Host 升级到 v2 持久连接，复用已启动的 PowerShell 进程。
- 安装脚本只在安装或文件缺失时解包 PowerShell，不再为每次点击启动额外解包进程。
- Native Host 可直接接收文件或目录路径；传入文件时打开其父目录。

完整说明见 [`docs/releases/1.5.32.md`](docs/releases/1.5.32.md)。

## 1.5.31 - 2026-07-21

- 保存任务列表当前页码和该页的纵向滚动位置。
- 刷新网页或收起后重新展开窗口时，恢复原任务页及浏览位置。
- 滚动位置采用节流写入，并在页面离开时补写最终值，减少扩展存储压力。
- 保存页码超过当前任务总页数时，自动回退到最后一页。

完整说明见 [`docs/releases/1.5.31.md`](docs/releases/1.5.31.md)。

## 1.5.30 - 2026-07-21

- 持久化悬浮窗口的展开/收起状态。
- 刷新网页后恢复刷新前的窗口状态，并继续使用已保存的位置和尺寸。
- 点击悬浮球、点击收起按钮及提交任务自动展开均统一更新状态存储。
- 首次安装仍默认显示为收起的悬浮球。

完整说明见 [`docs/releases/1.5.30.md`](docs/releases/1.5.30.md)。

## 1.5.29 - 2026-07-21

- 分离用户设定的面板尺寸与浏览器视口限制产生的临时显示尺寸。
- 浏览器缩小时面板继续自动适配，但不再把受限尺寸写回持久化配置。
- 浏览器恢复原大小后，面板会自动恢复到用户最后设定的宽高。
- 合并连续的浏览器尺寸事件，避免同一帧重复调整面板。

完整说明见 [`docs/releases/1.5.29.md`](docs/releases/1.5.29.md)。

## 1.5.28 - 2026-07-21

- 移除任务表格的全部内部分隔线、拖动引导线和列宽联动逻辑。
- 状态列固定为 `90px`、操作列固定为 `180px` 并保持贴右，名称列占满剩余宽度。
- 横向缩放只改变名称列可显示宽度，纵向缩放只改变任务列表可见高度。
- 不再读取或保存任务列宽，减少快速缩放时的重排、闪烁和卡顿。

完整说明见 [`docs/releases/1.5.28.md`](docs/releases/1.5.28.md)。

## 1.5.27 - 2026-07-21

- 外部窗口横向缩放时恢复表头、任务行和操作列的全表实时列宽更新。
- 移除松手后才同步表体的冻结策略。
- 保留 `requestAnimationFrame` 合帧和无同步布局读取的性能约束。
- 固定表头、表体的表格布局，并通过稳定滚动条占位减少列边界晃动。

完整说明见 [`docs/releases/1.5.27.md`](docs/releases/1.5.27.md)。

## 1.5.26 - 2026-07-21

- 横向缩放窗口时仅实时更新轻量表头，冻结包含全部任务行和按钮的表体横向几何。
- 松手后表体一次性同步最终临时列宽，降低缩放期间的重排、闪烁和卡顿。
- 垂直弹性布局保持实时，可见任务数量继续随窗口高度变化。
- 新增任务列表窗口与列宽交互的权威维护文档。

完整说明见 [`docs/releases/1.5.26.md`](docs/releases/1.5.26.md)。

## 1.5.25 - 2026-07-21

- 将外部窗口缩放产生的列压缩改为临时显示宽度，不再覆盖用户设定列宽。
- 窗口缩小时名称、状态列逐步压缩，重新放大时按原设定逐步恢复位置。
- 松手后仅保存窗口尺寸；原始列宽仍只由内部分隔线修改。
- 页面刷新、面板重新展开和浏览器视口变化时，根据当前窗口与原始列宽重新适配。

完整说明见 [`docs/releases/1.5.25.md`](docs/releases/1.5.25.md)。

## 1.5.24 - 2026-07-21

- 移除内部列分隔线改变外部悬浮窗口宽度和位置的联动。
- 分隔线向右拖动时仍依次压缩紧邻列和操作列，两者达到最小宽度后直接停止。
- 仅外部窗口边缘可以改变窗口尺寸；外部缩窗压缩名称列、状态列的逻辑继续保留。

完整说明见 [`docs/releases/1.5.24.md`](docs/releases/1.5.24.md)。

## 1.5.23 - 2026-07-21

- 修复悬浮窗靠近浏览器右边缘时，内部列分隔线无法触发扩窗的问题。
- 内部拖动所能扩展的宽度改为使用整个视口剩余容量，不再只计算窗口右侧空白。
- 右侧空间不足时，窗口在增宽的同时自动向左移动，直至视口左右边距。
- 内部扩窗结束后同时保存窗口宽度和左侧位置。

完整说明见 [`docs/releases/1.5.23.md`](docs/releases/1.5.23.md)。

## 1.5.22 - 2026-07-21

- 名称/状态分隔线向右拖动时，先压缩状态列，再压缩最右侧操作列。
- 右侧可压缩空间耗尽后继续拖动，会实时扩大整个悬浮窗口至视口边缘。
- 缩小窗口时允许名称列和状态列按可压缩空间共同收缩。
- 名称、状态和操作列均达到最小宽度后，窗口才停止继续缩小。
- 分隔线扩窗和窗口缩列后的尺寸与列宽均持久化保存。

完整说明见 [`docs/releases/1.5.22.md`](docs/releases/1.5.22.md)。

## 1.5.21 - 2026-07-21

- 列宽拖动反馈恢复为贯穿任务表格的蓝色长线。
- 拖动期间列标题和原有小分隔线保持不动，避免多处元素同时移动。
- 松手后一次性应用最终列宽，保留 1.5.20 的方向性逻辑及最小宽度约束。

完整说明见 [`docs/releases/1.5.21.md`](docs/releases/1.5.21.md)。

## 1.5.20 - 2026-07-21

- 重构分隔线方向性逻辑：只直接修改分隔线左侧紧邻列。
- 向右拖动时，中间列宽保持不变，最右侧操作列被压缩至最小宽度后停止。
- 向左拖动时，只缩小左侧紧邻列，不修改更左侧列宽，释放空间由操作列吸收。
- 实时预览时整体平移右侧列标题和后续分隔线，保持中间列宽视觉不变。

完整说明见 [`docs/releases/1.5.20.md`](docs/releases/1.5.20.md)。

## 1.5.19 - 2026-07-21

- 移除名称列 `900px`、状态列 `320px` 等人为最大宽度限制。
- 分隔线仅在相邻列达到最小宽度或操作列达到最小宽度时停止。
- 取消分隔线默认状态下额外绘制的灰线，避免与 Ant Table 原生边框形成双线。
- 调整拖拽热区位置，悬停和拖动时的蓝线与原生列边界重合。

完整说明见 [`docs/releases/1.5.19.md`](docs/releases/1.5.19.md)。

## 1.5.18 - 2026-07-21

- 修复表头 CSS 变量预览未稳定作用于 Ant Table 可见标题的问题。
- 拖动时直接对小分隔线及其右侧列标题应用 GPU 位移，确保实时跟随鼠标。
- 松手后将预览位置一次性固化为真实列宽并清除位移，不再等待 React 表格重建后才显示标题变化。

完整说明见 [`docs/releases/1.5.18.md`](docs/releases/1.5.18.md)。

## 1.5.17 - 2026-07-21

- 移除贯穿任务列表的列宽拖动引导线。
- 拖动时直接移动原有的小分隔线，并让列标题实时跟随。
- 拖动期间仅重排轻量表头，任务行在松手后一次同步，保留流畅度优化。

完整说明见 [`docs/releases/1.5.17.md`](docs/releases/1.5.17.md)。

## 1.5.16 - 2026-07-21

- 列分隔线拖动改为 GPU 合成引导线预览，松手时一次性应用真实列宽，避免整表逐帧重排。
- 窗口缩放期间冻结 Ant Table 表头与表体的横向几何，结束后一次同步，减少快速缩放时的标题晃动。
- 保留分隔线移动范围与操作列最小宽度约束，引导线在到达限制时同步停止。

完整说明见 [`docs/releases/1.5.16.md`](docs/releases/1.5.16.md)。

## 1.5.15 - 2026-07-21

- 操作列改由浏览器原生填充剩余宽度，移除缩放时的列宽观察与二次校正，解决分隔线晃动。
- 窗口缩放和表头分隔线拖动使用 `requestAnimationFrame` 合并更新，降低重排频率。
- 移除拖动过程中的同步尺寸读取，避免样式写入后立即强制布局。
- 隐藏 Ant Table 表体测量行中的重复拖拽线，仅保留可见表头分隔线。
- 拖动期间暂时关闭表格动画和过渡，减少整窗闪烁。

完整说明见 [`docs/releases/1.5.15.md`](docs/releases/1.5.15.md)。

## 1.5.14 - 2026-07-21

- 窗口最小宽度根据当前名称列、状态列及操作列最小宽度动态计算。
- 缩小窗口时，右边缘到达操作列最右侧后停止，避免表格横向越界。
- 拖动状态/操作分隔线时保留操作列最小宽度，到达限制后停止移动。

完整说明见 [`docs/releases/1.5.14.md`](docs/releases/1.5.14.md)。

## 1.5.13 - 2026-07-21

- 移除缩放期间逐帧修改表格高度的尺寸监听逻辑，解决拖动卡顿。
- 任务列表改为纯 CSS 弹性高度布局，随窗口尺寸原生实时伸缩。
- 分页栏固定在任务区域底部，列表主体独立滚动。

完整说明见 [`docs/releases/1.5.13.md`](docs/releases/1.5.13.md)。

## 1.5.12 - 2026-07-21

- 缩放悬浮窗口高度时，任务列表可视区域改为实时跟随。
- 使用尺寸观察器直接更新表格滚动区域，避免拖动过程中反复重渲染整张任务表。
- 搜索结果和普通任务列表继续使用各自的高度预留规则。

完整说明见 [`docs/releases/1.5.12.md`](docs/releases/1.5.12.md)。

## 1.5.11 - 2026-07-21

- 名称与状态之间的拖拽线改为相邻列分隔器。
- 拖动状态前方分隔线时，名称与状态列一增一减，操作前方分隔线及操作列保持原位。
- 更新分隔线提示文字，明确其控制的两侧列。

完整说明见 [`docs/releases/1.5.11.md`](docs/releases/1.5.11.md)。

## 1.5.10 - 2026-07-21

- 修复表格宽于列配置总宽时，Ant Design 按比例拉伸所有列而造成的分隔线联动。
- “操作”列自动吸收窗口剩余宽度；名称与状态列严格保持用户设置的像素宽度。
- 窗口缩放后自动重新计算操作列宽，空间不足时恢复为表格内部横向滚动。

完整说明见 [`docs/releases/1.5.10.md`](docs/releases/1.5.10.md)。

## 1.5.9 - 2026-07-21

- 修正表头分隔线拖动时相邻列边界被挤动的问题。
- 拖动列宽时改为直接更新表格 CSS 宽度，松手后再同步状态及保存，减少卡顿。
- 移除“操作”列末尾不必要的拖拽分隔线；仍可通过其左侧分隔线调整“状态”列宽。

完整说明见 [`docs/releases/1.5.9.md`](docs/releases/1.5.9.md)。

## 1.5.8 - 2026-07-21

- 展开悬浮窗口支持拖动右边缘、上边缘和右上角调整宽高。
- 窗口尺寸持久化保存，并根据当前浏览器视口自动限制。
- “名称 / 状态 / 操作”表头增加拖拽分隔线，可自由调整列宽。
- 列宽持久化保存；名称列变宽后可以显示更多文件名内容。
- 表格高度跟随窗口实际高度，列总宽度超出窗口时仅在表格内部横向滚动。

完整说明见 [`docs/releases/1.5.8.md`](docs/releases/1.5.8.md)。

## 1.5.7 - 2026-07-21

- 移除展开悬浮面板最右侧的外层滚动条，仅保留任务表格内部滚动。
- 表格可滚动高度根据浏览器窗口高度自动调整。
- 搜索结果和索引进度区域出现时自动为其预留垂直空间，避免分页被裁掉。

完整说明见 [`docs/releases/1.5.7.md`](docs/releases/1.5.7.md)。

## 1.5.6 - 2026-07-21

- 缩略悬浮球和展开窗口改为分别保存位置。
- 拖动展开窗口后再收起，小球保持在自身上次保存的位置。
- 兼容迁移旧版共享位置数据，升级时不会突然重置到默认位置。

完整说明见 [`docs/releases/1.5.6.md`](docs/releases/1.5.6.md)。

## 1.5.5 - 2026-07-21

- 禁止悬浮球图片触发浏览器原生拖拽，修复缩略状态无法移动的问题。
- 拖动过程改用窗口级 Pointer Event 监听，指针移出悬浮球后仍持续跟随。
- 缩略球的普通点击改在 Pointer Up 时识别，同时保留键盘点击支持。

完整说明见 [`docs/releases/1.5.5.md`](docs/releases/1.5.5.md)。

## 1.5.4 - 2026-07-21

- 缩略悬浮球支持鼠标、触控笔和触摸拖动。
- 展开面板支持从标题区域拖动，不影响面板内按钮、标签页和表格操作。
- 收起和展开状态共用左下角位置锚点，并通过扩展存储全局保存。
- 增加拖动阈值，避免拖动悬浮球后误触展开。
- 自动限制悬浮窗位置并响应窗口尺寸变化，防止面板移出可视区域。

完整说明见 [`docs/releases/1.5.4.md`](docs/releases/1.5.4.md)。

## 1.5.3 - 2026-07-21

- 搜索改为点击按钮或按 Enter 后执行，不再随输入自动发起索引请求。
- 新增搜索主按钮、索引百分比、已加载页数和已读取任务数。
- 索引完成后显示结果数量和当前搜索关键词。
- 清空搜索框会立即退出搜索并恢复普通任务列表。

完整说明见 [`docs/releases/1.5.3.md`](docs/releases/1.5.3.md)。

## 1.5.2 - 2026-07-21

- 任务第一页在网页空闲时轻量预取，首次展开面板可直接显示缓存。
- 列表请求完成后立即渲染，文件存在性检查改为限流后台任务。
- 简繁归一化结果随任务缓存，连续修改搜索词不再重复转换全部任务。
- 搜索索引支持请求合并、60秒失效和状态增量合并。
- 缓存采用数量上限：12个任务页、5000条完整索引和300条文件检查结果。
- 提交、删除、任务总数变化及活动任务轮询会让相关缓存失效或更新。

完整说明见 [`docs/releases/1.5.2.md`](docs/releases/1.5.2.md)。

## 1.5.1 - 2026-07-21

- 为离线任务列表增加跨全部分页的搜索功能。
- 支持简体关键词匹配繁体任务名，并兼容繁体、大小写和全角字符输入。
- 支持用一个或多个空格分隔关键词，所有关键词均命中时才显示任务。
- 搜索范围包含任务名称、磁力链接和 info hash。

完整说明见 [`docs/releases/1.5.1.md`](docs/releases/1.5.1.md)。

## 1.5.0 - 2026-07-21

- 默认关闭弹幕密度热力图，并保留设置开关。
- 持久化弹幕显示、发送样式及常用播放器偏好。
- 统一全屏和普通模式的控制栏自动隐藏行为。
- 修复播放器进度保存定时器的生命周期清理。
- 完善远程 MKV ASS/SSA 字幕的 libav.js 解封装、libass 渲染和 WebVTT 兜底。
- 默认使用弹幕直连模式，并修复代理路径编码问题。
- 增加可选的 Windows Native Messaging 本地目录功能。

完整说明见 [`docs/releases/1.5.0.md`](docs/releases/1.5.0.md)。
